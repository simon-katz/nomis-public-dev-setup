;;;; Init stuff -- ibuffer.

;; (require 'ibuffer)
;; (global-set-key (kbd "C-x C-b") 'ibuffer)
(setq ibuffer-default-sorting-mode 'alphabetic)

;;;; ___________________________________________________________________________

(provide 'nomis-ibuffer)
